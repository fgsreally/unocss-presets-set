{
  "name": "unocss-presets-set",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "changeset": "changeset",
    "dev": "pnpm -r --filter=./packages/* --parallel run dev",
    "build": "pnpm -r --filter=./packages/* --parallel run build",
    "publish": "changeset publish"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
    "@changesets/cli": "2.24.3",
    "typescript": "^4.8.4"
  }
}